language: python
python:
- 3.5
install:
- pip install -r requirements.txt
- pip install codacy-coverage
script:
- cd EjercicioDjango
deploy:
  buildpack: python
  provider: heroku
  app: mvp-tfg
  strategy: git
  api_key: $HEROKU_AUTH_TOKEN
  on:
    branch: deploy